{
  "name": "magnet-to-torrent-userscript",
  "version": "4.0.0",
  "private": true,
  "description": "为网页中的磁力链接添加 .torrent 下载按钮",
  "type": "module",
  "scripts": {
    "check": "node --check magnet-to-torrent.user.js && node --test",
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": ">=26.1.0 <27"
  },
  "engines": {
    "node": ">=18"
  }
}
